tpen-data-uri:
tpen-data-branch:

tpen2tei-uri:
tpen2tei-branch:

moe-data-uri:
moe-data-branch:

insecure-registries: "<ip of registry>:5000"
tools-image: <ip of registry>:5000/tools-final

version-uri:
version-branch:
version-file: ci/version

ssh-keys-uri:
netrc-test-api-uri:

relaxng_schema: http://www.tei-c.org/release/xml/tei/custom/schema/relaxng/tei_all.rng

stemmarest-sources-uri:
stemmarest-sources-branch:
stemmarest-config-uri:

# api-base:
# user-name:
# user-email:
# user-passphrase:
# tradition-name:
# trandition-lang:
